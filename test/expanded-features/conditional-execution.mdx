function TestComponent() {
    const showGreeting = true;
    const showWarning = false;

    return (
        {showGreeting && (
            Hello there!
        )}
        {showWarning && (
            Warning message
        )}
    )
}
