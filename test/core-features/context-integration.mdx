function ContextTest() {
  const { user, isLoggedIn } = useAuth();

  return (
    {isLoggedIn && (
      Welcome back, {{ user.name }}!
    )}

    {!isLoggedIn && (
      Please log in.
    )}
  )
}
