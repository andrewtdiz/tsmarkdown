function Test() {
  const items = ['Apple', 'Banana', 'Cherry'];
  const user = { name: 'Alice', age: 30 };

  return (
    Items:
    {{ items.map((item, index) => (
        - {{ item }}
    ))}}
  )
}
