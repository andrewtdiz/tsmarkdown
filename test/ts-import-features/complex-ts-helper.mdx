import { processData, validateData, formatResult } from './fixtures/complex-helper';

function ComplexTsHelper() {
  const data = 'data';
  const processed = processData(data);
  const validated = validateData(processed);
  const formatted = formatResult(validated);
  
  return (
    # Complex TypeScript Helper
    
    Processed: {{ processed }}
    Validated: {{ validated }}
    Formatted: {{ formatted }}
  );
}
