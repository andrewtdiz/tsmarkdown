import type { UserType } from './fixtures/types';
import { getValue } from './fixtures/runtime';

function TypeOnlyImports() {
  const value = getValue();
  
  return (
    # Type-only Import Test
    
    Runtime value: {{ value }}
  );
}
