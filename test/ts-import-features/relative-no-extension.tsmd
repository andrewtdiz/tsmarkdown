import { calculateValue } from './fixtures/calculator';

function RelativeNoExtension() {
  const result = calculateValue();
  
  return (
    # Auto-resolved TypeScript
    
    Function result: {{ result }}
  );
}
