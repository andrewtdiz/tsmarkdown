function MarkdownIntegration({ userName }: { userName: string }) {
  return (
    # Welcome {{ userName }}!

    This document demonstrates that **markdown formatting** works alongside TS Markdown features.

    ## Code Examples

    Here's some `inline code` and a code block:

    ```javascript
    function greet(name) {
        return `Hello, ${name}!`;
    }
    ```

    ## Links and Tables

    Visit [TS Markdown](https://www.tsmarkdown.dev) for more information.

    | Feature | Status |
    |---------|--------|
    | Interpolation | ✅ Working |
    | Conditionals | ✅ Working |
    | Markdown | ✅ Working |

    > This block quote shows that all features work together seamlessly.
  )
}
