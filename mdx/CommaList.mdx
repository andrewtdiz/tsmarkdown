function CommaList({ items, withAnd = true }: { items: string[]; withAnd?: boolean }) {
  const beginningItems = items.slice(0, -1);
  const lastItem = items[items.length - 1];
  
  return (
    {{ items.length === 0 ? (
      Empty
    ) : beginningItems.join(', ') + (withAnd ? ' and ' : ', ') + lastItem }}
  )
}
