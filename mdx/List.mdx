import OlItem from './OlItem'
import UlItem from './UlItem'

interface ListProps {
  items: string[];
  ordered?: boolean;
}

function List({ items, ordered = false }: { items: string[]; ordered?: boolean }) {
  return (
    {items.map((item, index) => ordered ? <OlItem item={item} index={index} /> : <UlItem item={item} />)}
  )
}
